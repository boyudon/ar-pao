// ============================================================
//  ตั้งค่าระบบ AR — แก้ไขค่าต่าง ๆ ได้ที่ไฟล์นี้ไฟล์เดียว
// ============================================================
window.AR_CONFIG = {

  // ---- ไฟล์เป้าหมายภาพ (.mind) ที่คอมไพล์จาก compiler.html ----
  // 🔴 ตอนนี้อยู่ใน "โหมด DEMO" ใช้การ์ดตัวอย่างของ MindAR
  //    ใช้งานจริง: เปลี่ยนกลับเป็น  "./assets/targets.mind"
  targetSrc: "https://cdn.jsdelivr.net/gh/hiukim/mind-ar-js@1.2.5/examples/image-tracking/assets/card-example/card.mind",

  // ---- วิดีโอท่าน ผอ. (ไฟล์ .mp4 H.264) ----
  // 🔴 โหมด DEMO: วิดีโอตัวอย่าง (มีเสียง)
  //    ใช้งานจริง: เปลี่ยนกลับเป็น  "./assets/director.mp4"
  videoSrc: "https://cdn.jsdelivr.net/gh/mdn/learning-area@main/html/multimedia-and-embedding/video-and-audio-content/rabbit320.mp4",

  // ---- ขนาด/ตำแหน่งวิดีโอบนภาพเป้าหมาย ----
  // videoWidth = 1 หมายถึง กว้างเท่ากับภาพเป้าหมายพอดี
  videoWidth: 1,
  // ความสูง: ใส่ null = คำนวณจากสัดส่วนวิดีโออัตโนมัติ (แนะนำ)
  videoHeight: null,
  // เลื่อนตำแหน่ง (x y z) หน่วยเทียบกับความกว้างภาพเป้าหมาย
  videoPosition: "0 0 0",
  // หมุน (องศา) เช่น "-90 0 0" ถ้าวางป้ายราบกับโต๊ะแล้วอยากให้วิดีโอ "ตั้งขึ้น"
  videoRotation: "0 0 0",

  // ---- เสียง ----
  // true = เปิดเสียง (ผู้ใช้ต้องแตะปุ่มเริ่มก่อน เบราว์เซอร์ถึงจะให้เล่นเสียง)
  sound: true,

  // ---- โหมดพื้นหลังโปร่งใส (สำหรับวิดีโอกรีนสกรีน) ----
  // ถ่าย ผอ. หน้าฉากเขียว แล้วเปิดค่านี้เป็น true เพื่อตัดพื้นหลังออก
  // ให้เห็นเฉพาะตัวท่าน ผอ. ยืนพูด (เหมือนโฮโลแกรม)
  // (โหมด markerless: เปิด chromaKey เพื่อตัดฉากเขียวออก ให้เห็น "ท่าน ผอ. ยืนลอย" ในห้องจริง)
  chromaKey: false,
  chromaColor: "#00d400",      // สีพื้นหลังที่จะตัดออก (เขียวมาตรฐาน)
  chromaSimilarity: 0.4,       // ความกว้างของช่วงสีที่ตัด (0.2–0.6)
  chromaSmoothness: 0.10,      // ความนุ่มของขอบ (0.0–0.3)

  // ---- ตำแหน่ง/ขนาดคลิปบนจอ (โหมด markerless: คลิปลอยทับภาพกล้อง) ----
  clip: {
    widthPercent: 80,    // ความกว้างคลิปเทียบกับความกว้างจอ (%)
    bottomPercent: 2,    // ระยะห่างจากขอบล่างจอ (%) — น้อย = ผอ. "ยืนที่พื้น"
    align: "center",     // center | left | right
  },

  // ---- ข้อความบนหน้าจอ (ปรับเป็นชื่อหน่วยงานของคุณได้) ----
  ui: {
    orgName: "องค์การบริหารส่วนจังหวัด",
    title: "ระบบ AR แนะนำข้อมูลโดยท่าน ผอ.",
    introMarkerless: "แตะปุ่มด้านล่างเพื่อเปิดกล้อง แล้วท่าน ผอ. จะปรากฏขึ้นทันที", // โหมดหลัก (index.html)
    intro: "แตะปุ่มด้านล่างเพื่ออนุญาตกล้อง แล้วเล็งกล้องไปที่ป้าย/ภาพเป้าหมาย",     // โหมดส่องป้าย (ar-marker.html)
    startButton: "เริ่ม / อนุญาตกล้อง",
    scanText: "เล็งกล้องไปที่ภาพเป้าหมาย",
    hintText: "ขยับมือถือส่องรอบ ๆ ได้เลย",
    loadingText: "กำลังเปิดกล้อง...",
  },
};
